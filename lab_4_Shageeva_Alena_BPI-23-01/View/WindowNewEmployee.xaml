<Window x:Class="lab_4_Shageeva_Alena_BPI_23_01.View.WindowNewEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab_4_Shageeva_Alena_BPI_23_01.View"
        mc:Ignorable="d"
        Title="WindowNewEmployee" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Фамилия:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="5,8,0,7" Width="200" HorizontalAlignment="Left"/>

        <Label Content="Имя:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="5,8,0,7" Width="200" HorizontalAlignment="Left"/>

        <Label Content="Дата рождения:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="tbBirthday" Style="{StaticResource TextBoxDate}"
                 Text="{Binding Birthday}"
                 Grid.Row="4" Grid.Column="1" Height="20" HorizontalAlignment="Left" VerticalAlignment="Center"
                 Margin="5,8,0,7" Width="130"
                 IsVisibleChanged="tbBirthday_IsVisibleChanged" />
        <DatePicker x:Name="ClBirthday" Visibility="Hidden" 
                    Grid.Row="4" Grid.Column="1" 
                    SelectedDate="{Binding Birthday, StringFormat={}{0:dd.MM.yyyy}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,8,0,7" Width="130" />

        <Label Content="Должность:" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CbRole" Grid.Row="7" Grid.Column="1" 
                  DisplayMemberPath="NameRole"
                  SelectedValuePath="Id"
                  Margin="5,8,0,7" Width="200" HorizontalAlignment="Left"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="10" Grid.ColumnSpan="2" Margin="0,20,0,0">
            <Button Content="ОК" Width="75" Margin="5" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Отмена" Width="75" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>